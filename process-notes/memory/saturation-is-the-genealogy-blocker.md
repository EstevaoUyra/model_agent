---
name: saturation-is-the-genealogy-blocker
description: "Divisive-normalization saturation is the shared, paper-underdetermined reproduction blocker across the attention-normalization genealogy (RH2009/heeger/hermann are facets of one human decision)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e8552c97-c722-4643-9557-00815e47fc70
---

The attention-normalization genealogy's central reproduction difficulty is **divisive-normalization saturation** (CRFs reaching the paper's plateau), and it is ONE shared problem, not N independent model bugs. Established 2026-06-04 by running the hardened full-pass on `heeger_1992` (the normalization root) and `hermann2010` (a direct RH2009 descendant) after RH2009 itself blocked.

Saturation is contested at **every level** of the genealogy:
- **Root — heeger_1992:** saturation/width is paper-underdetermined. p.184 "filter orientation range 60°" vs "cell tuning widths" can't both hold when plotted response ∝ Vₑ² (squaring narrows FWHM by √2). Which clause binds (Vₑ=60 vs Vₑ=84.853) is a real human choice → `SQ-005`.
- **Keystone — reynolds_heeger_2009:** `SQ-005` suppression-saturation CONTRACT_BUG; the 2D-plane fix makes S smaller not larger, so it can't deliver saturation. STUCK, routed to human spec-review.
- **Descendant — hermann2010:** built ON the **R&H reuse surface** (its calibration.yaml says so); Fig-1a response-gain saturation (`NF1`) is "STILL BROKEN" for the same mechanism reason. Its own note routes the fix back to "reynolds_heeger_2009 / the organizer: raise the R&H saturation gain."

Organizer implication: RH2009-SQ-005, hermann-NF1, and heeger-Vₑ are facets of **one human spec-review decision about the R&H normalization-saturation mechanism** — consolidate, don't resolve three times. Do NOT unilaterally raise the saturation gain; that is the blocked contract/mechanism decision (see [[organizer-doesnt-implement-trust-the-process]]). Same decision-class as the original RH2009 blocker. See also [[re-audit-after-every-model-change]].
