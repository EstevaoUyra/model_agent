# E1 narration bundle — audit-faithfulness agents, by intervention era
# Interventions: #5 (2026-06-02 redesign) · #56 (2026-06-14 teeth) · #73 (2026-06-28 local-curve harden)
# Each excerpt = verdict-bearing assistant narration. Provenance = [era | date | model | path].


## ERA A_early_post#5  (population 97 auditors, sampled 6)

### [A_early_post#5 | 2026-06-03 | verhoef_maunsell_2017 | 3b2b7a60-da9d-4ae5-bb82-a3a5b9885198/subagents/workflows/wf_6d6edb4c-cae/agent-a247b26d7b0a44c9f.jsonl]
- No `faithfulness-fix` branch. I'm on `main`. Let me explore the structure.
- The A-002 symmetric-beta assumption is well-documented and matches "parameters" (plural) and the Fig 5C schematic. This is a defensible disambiguation, not a divergence — and the impl matches A-002 exactly.

### [A_early_post#5 | 2026-06-03 | olshausen_field_1996 | 3b2b7a60-da9d-4ae5-bb82-a3a5b9885198/subagents/workflows/wf_9af44023-305/agent-a81479b0037ade37f.jsonl]
- On the faithfulness-fix branch. Let me explore the structure.
- The r1 audit was thorough. Round 2 fix targeted the Fig-8 ledger units (the NEW minor contract item from r1). Let me verify the r2 fix and re-render everything. Let me look at the r2 commit diff and key files.
- The r2 fix is purely an annotation change. Critically, the r1 finding flagged the fix_class as `contract` (minor, the units mismatch). The r2 fix resolves the *direction* of the mismatch by relabeling the ledger to cycles/patch. But I need to verify: the new note now says fx_axis_ticks `units: cycles/patch` while `audited: true` and `source: C-018` where the paper prints cycles/picture. This is a potential concern — an audited:true entry whose units annotation no longer matches the verbatim paper quote. Let me check the citation and re-render.
- 26 tests pass. The C-018 quote confirms the paper axis ticks are "0, 0.9, 1.8, 3.6 cycles/picture". Now let me examine the r2 ledger change critically and view the figures. The key question for r2: is changing `fx_axis_ticks units: cycles/picture` → `units: cycles/patch` on an `audited: true` entry consistent with the verbatim quote?
- **Fresh render**: The axis is labeled "cycles/patch on the paper's radial axis". The distribution has points spread out, but notably there are NO points near the origin (inside the innermost region) — the floor is clearly above ~0.8. This is the ILLUSTRATIVE finding the r1 audit identified.

### [A_early_post#5 | 2026-06-03 | carandini_heeger_movshon_1997 | 3b2b7a60-da9d-4ae5-bb82-a3a5b9885198/subagents/workflows/wf_9af44023-305/agent-a9f16162de0571cd6.jsonl]
- On the faithfulness-fix branch. Let me explore the structure.
- 32 passed (was 1 failed, 31 passed in r1). N1 regression is resolved. Let me verify the actual fix to the stage test and the phase_advance code.
- Now let me verify the equation transcription against the actual paper. I have knowledge of CHM 1997. Let me reason about the key equation carefully, because this is the central faithfulness risk.
- - **Euclidean form** `[c/√(σ²+c²)]ⁿ`: at low c, denom ≈ σ, so R ≈ (c/σ)ⁿ → grows as **cⁿ**. ✓ matches "responses grow as cⁿ". - **Heeger form** `L·c²/(σ²+c²)` (n=2 fixed, ratio not to power n): at low c, grows as c². Doesn't have free n. - **Naka-Rushton power-sum** `(Lc)ⁿ/(σⁿ+cⁿ)`: at low c, denom ≈ σⁿ constant, R ≈ (Lc)ⁿ/σⁿ → grows as **cⁿ**. ✓ ALSO matches "grow as cⁿ".
- So "grows as cⁿ" is consistent with both Euclidean and power-sum. But "the c in the denominator has a strong effect" — singular "the c", denominator containing a single additive c term (i.e. `σ²+c²` → "the c"), is more consistent with the Euclidean reading where the denominator literally contains `c²` as a term. Let me get a cleaner source — the PMC full text or a review that reproduces the equation.

### [A_early_post#5 | 2026-06-06 | pestilli_ling_carrasco_2009 | e8552c97-c722-4643-9557-00815e47fc70/subagents/workflows/wf_19edde3c-0d3/agent-aa96fe049887aa0ec.jsonl]
- Renders are deterministic and match committed. Now let me view the rendered figures alongside the paper reference figures. Let me look at the key result figures and the digitization references.
- Key finding confirmed. The model's endogenous (contrast-gain) difference curve has peak 0.233 at C=0.244 (good, matches digitized ~0.30 at ~0.26 in shape/location), BUT at C=1.0 the difference is still 0.0495, and at C=0.9 it is 0.058. The digitized reference (panel 1c) has the curve returning to ~0.009 at C=0.9. This is a real divergence in the figure: the contrast-gain hump does not return to baseline at high contrast.
- This is the documented "residual" finding. Let me quantify the digitized reference at high contrast and assess severity. The digitized peak is ~0.284 at 0.26, and at 0.9 it's 0.0093. The model peak 0.233 at 0.244, but at 0.9 it's 0.058 — about 6x the reference's high-contrast value, ~25% of the peak. Let me check the cause: the sweep ceiling is 1.0, so neutral (0.95) hasn't reached attended (1.0). Let me verify the root cause and what the paper itself shows.
- Root cause confirmed: with illustrative C50=0.3, β=2, at C=1.0 the curve has only reached C²/(C²+0.09)=0.917 (neutral) — not saturated. The contrast-gain difference doesn't return to ~0 because the sweep ceiling (C=1) doesn't saturate the Naka-Rushton with these illustrative parameters. Extending to C=3 would shrink the residual to ~0.006. This is an illustrative-parameter / sweep-ceiling artifact, scope=figure, GENUINE_DIVERGENCE. It matches the prior audit's minor note. The shape (interior peak, location ~0.24) is faithful.
- Figure 6 model matches the paper well: pCRF attended (red ~1.0), neutral (black ~0.65), unattended (green ~0.40), and the difference panel shows attended rising to +0.33 (paper ~+0.4) and unattended falling to ~-0.27 (paper ~-0.25). Response-gain ordering correct.

### [A_early_post#5 | 2026-06-03 | hermann2010 | 3b2b7a60-da9d-4ae5-bb82-a3a5b9885198/subagents/workflows/wf_b9729dce-a82/agent-a5f2115fb07a03c9b.jsonl]
- **Figure 1 vs paper:** Paper Fig 1a (response gain): attended (red) and ignored (blue) overlap at low contrast, then diverge with attended scaled up multiplicatively — both reach near-saturation at high contrast, attended above. The rendered Fig 1a shows red above blue with a large gap at high contrast (1.0 vs 0.685) — but in the paper, the response-gain panel has BOTH curves saturating, with the attended overshooting. Here ignored saturates at 0.685 (doesn't reach a plateau) — let me look more carefully. Actually the key signatures: Fig 1a = upward scaling (vertical arrows in paper), curves s
- In the paper Fig 1, the y-axis is "Normalized response" and both panels show the attended (red) reaching the top. This matches: attended pinned to 1.0. Good — that's the correct convention (attended is the higher curve, normalized so its max = 1.0). No inversion problem here.
- This is the central faithfulness check. The article_aware contract itself states for Fig 1b: "the curves **converge at high contrast** (similar asymptote)" and "in (b) ... the curves nearly meet at the top."
- But the **rendered Fig 1b shows attended=1.0 and ignored=0.857** — a 0.143 normalized vertical gap at the top contrast, and the curves are visibly NOT converging (they remain roughly parallel). The README even admits this: "Figure 1b does not fully converge at the highest contrast (~0.14 normalized residual gap)".
- Let me quantify the gap profile precisely for both panels to determine whether the contrast-gain signature (gap peaks at intermediate, shrinks at high) actually holds, or whether the gap is monotonically growing (which would be a response-gain shape in panel b — a divergence).

### [A_early_post#5 | 2026-06-03 | doostani_2023 | 3b2b7a60-da9d-4ae5-bb82-a3a5b9885198/subagents/workflows/wf_9af44023-305/agent-aad4d2f213c566a78.jsonl]
- Confirmed numerically. Pat−PatN = {V1:+0.118, LO:+0.211, pFs:+0.231, EBA:+0.260, PPA:+0.181} — exactly matching the corrected SQ-010 disclosure (Pat remains peak, +0.12 to +0.26). N<PN<P holds in all ROIs. Normalization r² ≈ 0.94–0.99 (pinned high, the circular signature; paper ≈0.47–0.62). All consistent with the r1 audit's findings and the corrected r2 disclosure. Let me confirm the circularity and check the figure images visually.
- These match the r1 audit's documented findings exactly, and round 2 corrected the disclosure to be accurate. Let me check the legend ordering and run the test suite, then check the other figures.
- **Panel (a) Response change:** Paper has Data≈Normalization growing toward EBA/PPA, with Weighted Avg (gray) and Weighted Sum (cyan) both near-zero. Rendered: Data≈Norm grow toward EBA/PPA (correct, Sum near-zero correct), BUT **Weighted Avg (gray) overshoots dramatically** (peaking at pFs 0.84, above normalization) — where the paper's avg is near-zero. This is the disclosed ILLUSTRATIVE constructed-result artifact.
- **Panel (b) Asymmetry:** Paper has Data≈Norm substantial, Weighted Sum (cyan) largest, Weighted Avg (gray) near-zero/slightly-negative. Rendered: Data≈Norm 0.52-0.71 (correct gestalt), Sum largest 2.1-2.2 (correct), BUT **Weighted Avg (gray) sits above normalization** (0.9-1.56) — where the paper has it near-zero. Disclosed ILLUSTRATIVE.
- The model-panel layout matches (no spurious panels, schematic panels are paper's own panel a/b tops which are conceptual). These are all disclosed-not-asserted. Let me check Fig 2 and Fig 5.

## ERA B_around#56  (population 59 auditors, sampled 6)

### [B_around#56 | 2026-06-13 | zhang_1996 | a6fd7e82-9088-4e49-8e88-9c0c3e990d90/subagents/workflows/wf_2b5ff7c9-5e3/agent-a9a19b4f3700f1e24.jsonl]
- Critical: the paper says "averages of |α sin θ| and |W'(θ)| are equal" — note it's |W'(θ)|, NOT |γ W'(θ)|. The spec/builder assumed the mean-match is against |γ W'| (with γ=-0.063). Let me check this carefully. This is the crux of the alpha_sin override.
- If mean-match is `mean(|α sin θ|) = mean(|W'|)`, then `α = mean(|W'|)/mean(|sin|)`. If against `mean(|γ W'|)`, then `α = |γ| · mean(|W'|)/mean(|sin|)`. The factor |γ|=0.063 is exactly the ~11.5x... wait, let me compute. Let me verify numerically and re-render first.
- The most important thing to scrutinize: the alpha_sin override. The prior audit and spec read the mean-match condition as `mean(|α sin θ|) = mean(|γ W'|)` but the **paper caption literally says**: "so that the averages of |α sin θ| and |W'(θ)| are equal" — wait, let me re-read the OCR carefully. Line 1342: "We chose 01 = 0.00201 so that the averages of Ia sin 01 and I$V'(0)l are equal."
- "I$V'(0)l" is OCR for `|W'(θ)|`. So the caption says match against `|W'(θ)|`, NOT `|γ W'(θ)|`. But the spec/audit assumed `|γ W'|`. Let me re-render and compute both interpretations and check against the digitized panel_6B_odd.
- The citation C-014 transcribes the caption as "|gamma W'(theta)|". But the raw OCR (line 1342) reads "Ia sin 01 and I$V'(0)l" — which is `|α sin θ|` and `|W'(θ)|`, with NO gamma visible. This is the crux. Let me look at the actual PDF caption to resolve which it is. Let me check the source image of figure 6 and read the digitized panel files.

### [B_around#56 | 2026-06-12 | pfister_gerstner_2006 | a6fd7e82-9088-4e49-8e88-9c0c3e990d90/subagents/workflows/wf_05b2e3ba-49f/agent-aa551e88d5574feca.jsonl]
- Now let me look at the spikes builder and calibration. The Fig 7A λ mapping is something to scrutinize — paper says λ=0.64 dashed, λ=1.44 solid, but the code maps 0.64→upper dashed, 1.44→lower dashed and adds a λ=1.00 solid middle.
- The model's three-curve assignment matches the digitized minima extremely well: λ=0.64→min −0.471 (digitized −0.4, θ≈12 vs 14), λ=1.00→−1.15 (digitized −1.12, θ≈19 vs 20), λ=1.44→−2.384 (digitized −2.24, θ≈28 vs 30). So the model is internally consistent with the digitized figure.
- Confirmed: the panel shows THREE curves (one solid + two dashed). The upper dashed is shallowest, solid is middle, lower dashed is deepest. The caption only names two λ (0.64 dashed, 1.44 solid), which is inconsistent with the 3-curve figure. The spec's resolution (0.64=upper dashed shallowest, 1.00=solid middle, 1.44=lower dashed deepest) matches the figure data and BCM physics. This is a defensible figure↔caption reconciliation, not a code bug. I'll note it as a documented PAPER_ISSUE that the model handled correctly.
- The views entrypoint exists but only renders figures 3,4,5,7 — Figures 2 and 6 (which the paper has) are not rendered. Let me check the spec for which figures are in scope, then look at the rendered outputs.
- Figure 7A matches the digitized green points beautifully and reproduces the paper panel shape (3 curves, correct ordering, magnitude on ×10⁻⁵ axis). Figure 3 shows the correct biphasic STDP window. Note Fig 3 in the paper overlays the full vs minimal triplet model — let me check the paper's Fig 3 has blue=A2A vs red=NS. Actually paper Fig 3 caption says "for the full triplet model (A) and minimal (B)". The paper doesn't specify A2A vs NS coloring for Fig 3 explicitly the way it does for Figs 2/4/5 — but it's a reasonable presentation. Let me look at the Fig 3 overlay reference and Figs 4/5.

### [B_around#56 | 2026-06-12 | pfister_gerstner_2006 | a6fd7e82-9088-4e49-8e88-9c0c3e990d90/subagents/workflows/wf_32a62589-685/agent-aee30c7cc50cd81fc.jsonl]
- The README says the model is blocked at Phase A with no implementation. But the git log shows a recent commit "Phase B: build pfister_model triplet-STDP package; Fig 7A BCM mapping 8/12 green" on branch `fix/figure7a-lambda-20260612`. The README is stale. Let me check the actual state of the implementation and which branch I'm on.
- figure_outputs is empty. The package exists but no views/measurements and no rendered figures. Let me look at all the implementation source files and the paper text.
- This is a thin implementation. There's only the Fig 7A BCM closed-form path; no spike simulation, no views.py, no measurements.py, and no rendered figures. The scope claims 6 figures (Fig 2-7) but only Fig 7A has any code. Let me check the paper text for the equations and the BCM mapping (EQ-6), Table 3/4, and the Fig 7A specifics.
- Critical finding emerging. The paper explicitly states Fig 7A has **two** curves: dashed λ=0.64 and solid λ=1.44 (caption line 374, and the BCM mapping text line 308, line 310-311). But the implementation builds **three** curves (λ ∈ {0.64, 1.00, 1.44}). Let me read the Fig 7A digitization and the re-audit log to understand the basis for the three-curve claim.
- There's a serious concern here. The paper caption explicitly says Fig 7A is **two curves** (dashed λ=0.64, solid λ=1.44). The auditor decided it's actually three curves by looking at the digitized panel crop, then introduced a λ=1.00 solid and reassigned the dashed values, AND changed the A₂ scaling from the paper's √λ (since p=2, ⟨ρᵧ⟩/ρ₀=√λ) to λ to fit the digitized panel. This is fitting the contract to the digitized image rather than the paper. Let me examine the digitized panel and the source figure directly.

### [B_around#56 | 2026-06-13 | pfister_gerstner_2006 | a6fd7e82-9088-4e49-8e88-9c0c3e990d90/subagents/workflows/wf_c1423ea5-08f/agent-a524f1ef1a7fbf757.jsonl]
- There's a substantial descope history. The two key divergences (Q-005 Fig 2 pair model, Q-006 Fig 6 A2A swept-ρ) have been disposed as NOT-REPRODUCIBLE-FROM-RELEASED. Let me read the most recent independent faithfulness audit and the spec_questions to understand the state, then verify independently.
- The render matches the digitized points well (green dots overlaid lie on the curves). The shape, threshold ordering, and minima are correct. Let me verify the Fig 7A render against the actual paper figure crop, and look at Fig 2, Fig 4, Fig 6.
- This is the crux. The digitized Fig 2A reference shows the **pair model** curves (blue=A2A solid Δt+10) sitting at a **flat plateau ≈ +0.13** across all frequencies — the digitized JSON confirms `all_to_all_dt_plus10` is ~0.13-0.12 flat. But the **render** shows the A2A solid curve dropping from 0 down to −0.67 (sign-wrong, frequency-DEPENDENT LTD).
- This is exactly the Q-005 divergence. The build's "pair = full triplet fit with A₃=0" produces the wrong curve. The paper's Fig 2 pair model is a separate lsqnonlin fit with its own A₂± that is unreleased. The render's Fig 2A diverges materially from the paper. This is a genuine divergence, correctly descoped.
- Let me now verify the Q-006 (Fig 6 A2A) divergence and look at the rendered Fig 6 and Fig 4.

### [B_around#56 | 2026-06-12 | pfister_gerstner_2006 | a6fd7e82-9088-4e49-8e88-9c0c3e990d90/subagents/workflows/wf_32a62589-685/agent-ae619c9c6f1eb049c.jsonl]
- The model is contract-blocked and there's no views.py — figures aren't rendered by a model package. Let me understand how figures get produced. The skill says render entry point produces `figure_outputs/figure_<N>.png`, but that dir is empty. Let me look at the implementation source and how figures are meant to render.
- Only figure 7 is implemented; figures 3/4/5/6 protocols are not. There is no views.py and figure_outputs is empty. Let me check the prior faithfulness audit and the test/render entry points.
- There's a prior faithfulness audit. Now I need to re-render the CURRENT state and re-audit. The key question: has the contract been fixed since? Let me check the current state of the ledger and code. First, the ρₓ value, the dashed→λ assignment, and s=λ. Let me re-render.
- The ρₓ fix (→0.01) was applied to calibration.yaml since the prior faithfulness audit (cbba325). Let me check the current calibration value used by the code, and whether the model_spec.yaml stale ρₓ=1.0 was noted. Let me try rendering figure 7.
- The calibration ledger now has rho_x_hz: 0.01 (the F3 fix landed), but model_spec.yaml:429 still has 1.0 — a stale duplicate. Code reads from calibration.py (the ledger), so the effective value is 0.01. Let me verify which the code reads and confirm calibration.py merges from calibration.yaml.

### [B_around#56 | 2026-06-14 | None | d02bb335-2fe6-423d-a35c-a6974080e4da/subagents/workflows/wf_8ba0d4dc-127/agent-aa0bf9c37d2cbd805.jsonl]
- It's at `models/fitzhugh_1961`. But my instructions said `/Users/estevaouyra/dev/model_agent/fitzhugh_1961`. Let me check both.
- The actual path is `models/fitzhugh_1961`. My prompt's path is wrong but the model exists. I'll work in the real location. Let me explore the structure.
- Comparing Figure 1. The rendered figure has correct nullclines (N-cubic, slope -1 at origin), P at (1.20, -0.624) on intersection, dotted phase line. The paper's x-nullcline (ẋ=0) is the N-shaped cubic, y-nullcline (ẏ=0) the straight line. These match structurally.
- One thing to note: in the rendered figure the x-nullcline is labeled "y = -x + x³/3 - z" (blue) and matches the paper's ẋ=0 N-cubic. The y-nullcline (red straight line, slope -1/b = -1.25) matches. P sits at the intersection. Good structural match. The paper's trajectories show the clockwise circuit; the rendered trajectories are a generic seed grid — acceptable as A-006 representative trajectories.
- The Figure 2 render is small. There are important discrepancies I can already see. Let me look more carefully — the paper's panel 2B (z=-0.4 train) shows only ~3 spikes over t=0..500, with a long slow recovery phase between spikes (period ~110 time units). The rendered 2B shows MANY closely-packed spikes (~18 over 0..400). Let me render each panel larger to inspect period/count carefully.

## ERA C_post#56  (population 26 auditors, sampled 6)

### [C_post#56 | 2026-06-15 | bienenstock_cooper_munro_1982 | a651000f-a156-41c6-b8cc-3db390b5524f/subagents/workflows/wf_b4c958b9-2de/agent-a4e7a66e31b1a9dac.jsonl]
- The most recent prior faithfulness audit is `9acba43` (the independent re-audit). Since then, the changes are commits `70f2fdb`, `4f98ed7`, `e93cfc7`, `f785241`, `5be10fa`, `9d1b871`. Let me see what changed since that prior audit, and read the prior audit.
- The prior re-audit found PARTIAL with critical divergences. The diff since then shows: README, figure artifact PNGs (digitization references + reproduced), two test files, and metadata. Crucially, **no implementation/src changes** — the model code did not change. Let me verify that precisely.
- Now I understand the test re-grounding. The key change in `70f2fdb`: Finding-1 and Finding-5 tripwires were **re-grounded from an unsourced absolute-magnitude bound (peak ≤ 3×c0) to a non-convergence/runaway detector** (the run trips the divergence guard early). This is a legitimate concern from the auditor's perspective: the prior tests compared arbitrary multiples of a nuisance gauge (the paper fixes no absolute response magnitude per C-008/C-015), and the "9.99 peak" was just the guard-freeze value. The re-grounding makes the tripwires detect non-convergence directly.
- This is a **test/scaffolding change, not a model change** — the underlying divergences (Findings 1-5) are still XFAIL (still divergent). The re-grounding does not launder anything green; the tripwires remain RED (xfail strict). Let me verify the model code is byte-identical and the figures are render-identical, then run the suite.
- Render-identical (hashes match exactly before/after re-render) — the figures are deterministic and unchanged, consistent with no model change. But the suite shows **5 XPASS** — and the prior re-audit warned about XPASS(strict)→FAIL. Let me check whether these XPASS are under `strict=True` (which would FAIL) or non-strict. The suite reports "5 xpassed" as passing, meaning they're non-strict. Let me identify which tests xpass, since the prior audit flagged stale tripwires that XPASS strict→FAIL.

### [C_post#56 | 2026-06-15 | boynton_2009 | a651000f-a156-41c6-b8cc-3db390b5524f/subagents/workflows/wf_5abd222b-313/agent-a22f0d1e63116a110.jsonl]
- Now let me see what changed since this prior pass (2026-06-15.md, commit 9b40d70) up to HEAD.
- All four figures rendered. The exit code 1 is from the glob no-match in zsh, not the render. Let me check render identity and run the suite.
- Renders are byte-identical (no model change confirmed). The suite is now 21 passed + 2 xfailed (was 35 passed + 3 xfailed in prior audit — the count differs because the prior ran the full repo; this run is just `article_aware/extracted_data/`). The F-2 test flipped from xfail to passing (now 2 xfailed instead of 3). Let me verify exactly which tests xfail now and confirm the F-2 test is now a passing MUST-PASS.
- The two remaining xfails are F1 (Fig.1 floor) and F2 (Fig.2 dropped panel) — both still OPEN as before. F-2 test now PASSES. Now I must verify the F-2 fix is legitimate, not a laundered contradiction. The key risk (Step 4): did they "close it by raising d to fit the figure" — which issues.yaml explicitly forbade — or did they correctly lower the threshold per ladder rung 1?
- The digitized values are confirmed. attended_points @0% = 0.462, unattended_points @0% = 0.436 → gap 0.026 (and the fitted curves give 0.45−0.425 = 0.025 at c=0). So the paper's own Fig.2B does exhibit a nonzero c=0 gap of ~0.025-0.026, and the prior 0.05 threshold genuinely exceeded it. Lowering to 0.02 is correct: below the gap (paper's figure clears it) while still positive (a gain with ~0 effect at c=0 fails it). This is ladder rung 1 — the paper's own digitized figure. Legitimate resolution, not laundered.

### [C_post#56 | 2026-06-15 | ghose_maunsell_2008 | a651000f-a156-41c6-b8cc-3db390b5524f/subagents/workflows/wf_1c622463-d61/agent-adcce6f36c63e15a0.jsonl]
- The diff is purely tests, README, nodigitize markers, and logs — no model code, no figure-generation code. Per the diff-scoped re-audit rule: no scope=model change, so I don't need to deeply re-derive all figures. I need to (a) re-render and confirm output identity for the figures, (b) verify the test suite is green, (c) check the test-file change didn't launder anything, and (d) confirm the README relabel matches the prior audit's findings.
- The test changes are a faithful encoding of the prior audit's findings — the F1/F2 tripwires were converted from strict-xfail to passing document-consistency guards because the README was honestly relabeled to ILLUSTRATIVE. The new finding-4 tests guard non-digitize routing and include a complementary anti-laundering check (Fig 6 must NOT be over-routed to n/a). This is sound, not a laundering.
- Figure outputs are byte-identical after re-render — confirming the figures are unchanged from the prior audit (which found the model FAITHFUL but Figs 6/7 mislabeled). Now run the test suite and check the README relabel.
- 35 passed, 0 xfailed/failed. The prior audit reported 30 passed + 2 xfailed; now the 2 strict-xfail tripwires were converted to passing guards plus 3 new routing tests. Let me verify the README headings for Figs 6/7 now carry ILLUSTRATIVE-NOT-REPRODUCED and the n/a routing cells, confirming the relabel resolves F1/F2.
- The prior audit's two live divergences (F1/F2: Figs 6 & 7 mislabeled "✅ faithful") have been resolved by the honest README relabel to ILLUSTRATIVE-NOT-REPRODUCED, and the relabel tripwire tests now pass as permanent document-consistency guards.

### [C_post#56 | 2026-06-15 | boynton_2009 | a651000f-a156-41c6-b8cc-3db390b5524f/subagents/workflows/wf_5abd222b-313/agent-a3473b4a2d3e10bb4.jsonl]
- The model reads parameters from a frozen-fit artifact (`boynton_fits.json`), NOT from `spec_questions.yaml`. The Q-005 fix is a documentation/contract bookkeeping change only. Let me confirm the artifact's Fig.7 omega value and whether it matches the paper's true omega (78.5). This determines whether the model itself uses a wrong width.
- This confirms the change is scope=figure-bookkeeping/contract-documentation only, and no model code changed. Per the diff-scoped re-audit protocol: no model-scope change, so I do a deep re-audit only on figures whose *code/contract* changed. The Fig.7 contract (the recorded omega evidence) "changed" but it doesn't flow to the model. Still, I should verify Fig.7 renders correctly and the test suite is green. Let me run the test suite and re-render.
- 35 passed, 3 xfailed. The 3 xfailed are the known OPEN findings (F1 Fig.1 floor, F2 dropped panel 2A, F-2 threshold gap) — these are the documented divergences, deliberately left RED/xfailed. Let me re-render all figures and check the test_audit_findings to understand the xfailed findings, then deep-compare against the paper. Let me re-render now.
- Now I understand the full picture. The F-1 contract-bug fix (Q-005 omega 67.3→78.5) is now green. The 3 xfailed are documented OPEN divergences. Let me verify the F-1 fix is paper-grounded by checking the paper's Table 1 directly, and re-confirm the prior audit's findings on the figures still hold. Let me read the prior audit report and the paper's Table 1.
- Now I need to confirm: does this omega value actually affect any figure? The artifact uses illustrative `w=90.0` for the Fig.7 study, and Q-005's resolution A-005 explicitly treats omega as frozen-fit/evidence-only (not ported). So the contract fix does NOT change Fig.7's render. Let me verify Fig.7 render identity vs the prior committed render and visually confirm all four figures still match the paper.

### [C_post#56 | 2026-06-15 | carrasco2021 | a651000f-a156-41c6-b8cc-3db390b5524f/subagents/workflows/wf_fdd13a8b-747/agent-a36a63184fcc3e680.jsonl]
- I'm on branch `fix/full-pass-20260615`, 2 commits ahead. Let me find the prior audit and the diff since then.
- The diff since the prior pass (origin/main..HEAD) only touched Supp-Fig-4: a new test file, the regenerated PNG, and test logs. No model/forward-mechanism code changed. So per the diff-scoped instructions, my DEEP re-render+compare focuses on Supp-Fig-4. Other figures only need a render-identity check if they were FAITHFUL last round. Let me read the prior audit and the changed test, plus the repo structure.
- The prior audit (audit-2026-06-10) is comprehensive: it audited the whole model and found D1 (Fig 7 FAITHFUL), D2 (Supp-Fig-4 ILLUSTRATIVE scale divergence), D3 (S2 NMA provenance). The current branch's only change since origin/main: a new Supp-Fig-4 test file (the absolute-magnitude tripwire encoding D2), the regenerated PNG, and test logs.
- This is purely a figure-scope change (Supp-Fig-4 test/render), no model code. Let me verify: the diff is against origin/main, but the prior audit was at d8e77f8. Let me check what changed between the audited HEAD and now, and confirm no model code drifted.
- - **Figure 7**: unchanged + FAITHFUL last round → render-identity check suffices. - **Supp-Fig-4**: PNG regenerated + new test → deep re-render + compare.

### [C_post#56 | 2026-06-27 | vicente_kinouchi_caticha_1998 | 18b29086-79a1-4fb8-82a9-d0f7286c4e76/subagents/workflows/wf_432df3b2-8ee/agent-a00c205225a7911a4.jsonl]
- Tests pass (35 passed). Now let me verify the Fig 8 deadlock root cause precisely and confirm the consistent-rho fix would resolve it.
- Audit complete and committed to the model repo (`logs/faithfulness_audit/2026-06-27.md`, commit `4768880`).
- Summary: the forward model is faithful — every modulation function and ODE was verified operator-by-operator against the PDF, and Figs 2-6 reproduce the paper's shapes, ordering, plateaus and absolute magnitudes. The one substantive bug is a figure-scoped tabula-rasa deadlock in Fig 8's adaptive Annealed-Hebb/Step series.

## ERA D_post#73  (population 4 auditors, sampled 4)

### [D_post#73 | 2026-06-28 | vicente_kinouchi_caticha_1998 | 18b29086-79a1-4fb8-82a9-d0f7286c4e76/subagents/workflows/wf_d5723551-9fe/agent-aeee31454a61332d7.jsonl]
- Renders match the committed PNGs. Now let me numerically verify the fix claim and check curve fidelity through λ=1.
- The fix is verified: M matches analytic to ~1e-15 through λ=1, AH stationary now 0.24074 (analytic 0.24074, paper ~0.24). Now let me check the test suite result and trace all algorithm curves densely against digitized references.
- All asymptotic curves now monotone, no flat steps. Let me check test result and view rendered figures against the paper/digitized overlays.
- Fig 5 now: AH theory tracks its own triangles, no plateau. Fig 6: smooth, no steps. Let me view Figs 3/4/7 and the paper references plus the A-4 exponent question.
- Figs 5 and 6 now match the paper closely (plateaus AH~0.241/Sym~0.226/Step~0.211/Opt~0.196; smooth Fig 6). Let me check the A-4 exponent question against the paper, since the fix changed the fits.

### [D_post#73 | 2026-06-28 | vicente_kinouchi_caticha_1998 | 18b29086-79a1-4fb8-82a9-d0f7286c4e76/subagents/workflows/wf_d5723551-9fe/agent-a367938be306a1472.jsonl]
- The fix correctly targets the paper's exponents. Paper Table I (random): AH D^1/4, Sym/Step/Opt D^1/3. Table II (deterministic): AH D^1/6, Sym/Step/Opt D^1/4. The new legend reads match these. Let me write the report and commit.
- All checks complete. The diff-scoped re-audit confirms the round-2 minor legend finding is resolved, with no new divergences.

### [D_post#73 | 2026-06-28 | vicente_kinouchi_caticha_1998 | 18b29086-79a1-4fb8-82a9-d0f7286c4e76/subagents/workflows/wf_863fd7e6-332/agent-a3fbe24b7e53e0586.jsonl]
- Independent verification complete. The deterministic-coupling integration (peak 0.429, mean 0.367) is clearly incompatible with the digitized envelope (peak 0.296, mean 0.251), while the random reading matches — so the A-009 resolution is a genuine within-paper (rung-1) falsification, not laundering. Let me write and commit the audit report.
