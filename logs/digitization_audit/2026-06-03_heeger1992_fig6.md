# Digitization audit — heeger_1992 Figure 6 (panels A & B)

- Date: 2026-06-03
- Auditor role: figure-digitization critic (audit-digitization). NOT the digitizer.
- Model: models/heeger_1992 @ 04a9717 ("Digitize Figure 6 (both panels) — first digitization")
- Inputs read:
  - Paper figure: `article_aware/figures/figure_6.png` (page thumbnail) and the
    higher-res `article_aware/figures/figure_6/figure_6_source_crop.png` (400-DPI PyMuPDF
    crop the calibration is keyed to).
  - Digitized JSON: `panel_a_digitized.json`, `panel_b_digitized.json` (both carry full
    `provenance` blocks).
  - Spec/desc: `figure_6.md`, `figure_6_visual_checklist.md`.
  - Shipping overlays: `panel_a_overlay.png`, `panel_b_overlay.png`.

## Method (what I actually did — not an empty pass)

1. Re-rendered both overlays myself from the JSON anchors via `build_calibration` +
   `overlay` on the source crop (`.agent_tmp/myoverlay_A.png`, `_B.png`) — did not trust
   the committed overlays. My fresh render is pixel-coincident with the shipping overlays.
2. Round-tripped the calibrations: panel A anchors recover x=19.8/139.9 at the 20/140
   ticks; panel B recovers 9.9/129.8 at 10/130 — calibration is sound (<0.2 deg).
3. Adversarial zoom (`crop_region`, axis-coords, 4x) on the binding loci: panel B apex
   (70–110 deg × 0.25–0.30), panel B left base-alone crossing (40–75 deg × 0.14–0.22),
   panel A apex (70–100 deg × 18–30).
4. Independent pixel re-measurement (scipy, my own code, not the digitizer's): topmost
   dark ink per digitized x in panel B; dotted-line row average for both base-alone
   lines; apex marker row/col; panel A peak marker location.

## Panel B (6B — the MODEL reproduction target) — FAITHFUL-DIGITIZATION

This is the only model-output panel; it is the ruler the tier tests will measure against.

Independent per-marker re-measure (my topmost-ink y at each digitized x) vs the stored
points — agreement is essentially within scan noise:

| x (deg) | my y | digitized y |
|--------:|-----:|------------:|
| 29.4 | 0.145 | 0.147 |
| 37.7 | 0.154 | 0.150 |
| 46.4 | 0.169 | 0.166 |
| 63.0 | 0.243 | 0.240 |
| 71.8 | 0.269 | 0.273 |
| 80.1 | 0.290 | 0.287 |
| 89.1 | 0.295 | 0.292 |
| 97.5 | 0.291 | 0.287 |
| 105.7| 0.272 | 0.273 |
| 114.9| 0.242 | 0.242 |
| 123.4| 0.197 | 0.197 |
| 131.8| 0.169 | 0.168 |
| 140.8| 0.152 | 0.153 |

(54.8 deg not tabulated — my column-strip min caught a rising-segment edge, not the
marker; the overlay shows that marker on-ink.)

Binding dimensions all check out against the paper:
- Axis calibration: linear x [10..170], linear y [0..0.3]; anchors round-trip cleanly.
  No log/linear confusion, no offset origin, no swapped range.
- Apex: ~0.292 at ~88–89 deg (my densest-top measure ~87.7–89; dotted-row apex y 0.291).
  The zoom shows three near-equal top markers (~0.287/0.292/0.287) — a ROUNDED top, not a
  pointy spike. Digitization preserves this (does not over-sharpen).
- Base-alone (dotted): my dotted-row average y ≈ 0.185–0.186; stored 0.185. Exact.
- Normalization: stored AS DRAWN on the 0..0.3 "arbitrary units" axis — peak 0.292,
  base 0.185 — NOT per-panel renormalized to 1.0. This is the correct call (the figure's
  point is enhancement = peak ABOVE base, which a renormalize-to-1 would destroy).
- Enhancement band: curve crosses base-alone near ~58 deg and ~120 deg → ~60 deg broad,
  matching the checklist's broad-band expectation (not a narrow ~40 deg spike). Above near
  center, below at the edges — crossing on both flanks, as the figure requires.
- Completeness/mapping: both elements present (solid bell + dotted base-alone), correct
  color/style→condition mapping in the overlay legend.

Minor, disclosed, non-divergent: the apex marker sits at ~89 deg while the extreme-marker
symmetry center is ~85 deg (~1–4 deg). This is within marker-centroid + calibration scan
error and the digitizer flagged it explicitly; it is recorded as-drawn rather than snapped
to 90. Not a divergence.

## Panel A (6A — EMPIRICAL Bonds-1989 reference, NOT a model target) — FAITHFUL-DIGITIZATION

Scope note: per figure_6.md and A-008, panel A is empirical data, not a model-reproduction
target; it carries no model-faithfulness obligation. I audited it only as a faithful trace
of the paper ink.

- 6 markers connected by STRAIGHT segments (Bonds measurements). Digitizer correctly stored
  the 6 vertices and noted that the overlay's PCHIP rounding of the 29 sp/s apex and the
  slight bulge on the 83→114 descending limb are RENDER artifacts, not data. The zoom
  confirms a sharp straight-segment apex in the paper — the data points are right; only the
  PCHIP render rounds it. Correct disclosure.
- Peak: my pixel measure ~29.1 sp/s at ~81–83 deg vs stored (83.5, 29.0). Within fat-marker
  centroid error.
- Base-alone: my dotted-row average ≈ 14.1–14.3 sp/s vs stored 14.2. Exact.
- Axis: linear x [20..180], linear y [0..30] (spikes/sec, NOT normalized/shared with B).
  Correct — kept in physical units, not co-scaled with the arbitrary-units B panel.

## Process (Step 2) — adequate, recorded

- Both panels carry a full `provenance` block: figure_type, tools, tool_rationale,
  calibration anchors + crop_note, per_curve method, normalization, caveats.
- Figure type correctly identified as Mode-1 DISCRETE-MARKER plots (filled circles joined
  by segments), not continuous curves. Right read: marker centroids, NOT per-column
  `trace_darkest_in_band` — and the rationale explicitly records WHY trace_darkest was
  rejected (marker disks + dotted line contaminate any band; apex flickered 0.27–0.29
  column-to-column). This is exactly the composable/right-tool reasoning the skill asks for.
- Method is dark-density-peak centroiding (scipy uniform_filter), an extraction, not
  eyeballing. The eyeball tells are ABSENT: x-values are non-round (29.4, 37.7, 46.4 …),
  not a dozen evenly-spaced hand-typed numbers; calibration step is present and corner-
  verified; overlay validation was performed. My independent re-measure reproducing the
  stored y to ±0.003 confirms the data was extracted, not estimated.
- No silent fallback: where trace_darkest didn't fit, the digitizer named the substitution
  rather than forcing the wrong tool.

## Refute pass (Step 3)

- "Apex off-center (~89 vs symmetry ~85)" → could be mis-calibration? No: calibration
  round-trips cleanly and base-alone/feet/flanks all land; it is real marker-position
  scatter in the scan, disclosed. Downgraded to non-finding.
- "Panel B apex looks flat/rounded — under-sampled plateau?" → No: the paper itself shows
  three near-equal top markers; the rounding is in the paper, faithfully preserved. Not a
  divergence.
- "Panel A apex rounded in overlay" → render-only (PCHIP); stored vertices are a sharp
  straight-segment peak. Disclosed correctly. Not a data finding.

## Verdict

- Panel 6B (model target): **FAITHFUL-DIGITIZATION** — calibration sound, all 14 markers
  + base-alone independently reproduced to scan precision, recorded AS-DRAWN (no
  renormalize-to-1), broad crossing band preserved, produced by an auditable extraction.
- Panel 6A (empirical reference): **FAITHFUL-DIGITIZATION** — faithful straight-segment
  trace of the Bonds data; out of model-faithfulness scope by design.

No DIGITIZATION-DIVERGENT, no TOOL-MISUSE, no BLOCKED, no UNVERIFIED items.

Per the skill I do not mark the reference "trusted" or green the figure — routing/gating
is the organizer's. This report records that the Figure 6 ruler survives an independent,
adversarial overlay + zoom + pixel re-measure.
