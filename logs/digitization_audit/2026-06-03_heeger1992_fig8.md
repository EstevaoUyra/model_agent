# Digitization audit — heeger_1992 Figure 8 (panel B)

Date: 2026-06-03
Auditor: figure-digitization critic (independent of the digitizer)
Scope: `models/heeger_1992/article_aware/figures/figure_8/panel_b_digitized.json`
vs the paper figure (`figure_8.png`) / panel crop (`panel_b_crop.png`).
Panel A (Bonds 1989 empirical data) is correctly NOT digitized — out of scope.

## Verdict

**Panel B: FAITHFUL-DIGITIZATION.**
All four curves match an independent paper-pixel re-trace to within marker-size /
open-marker-midpoint tolerance; calibration (log-x, log-y) independently confirmed;
the shipping overlay (PCHIP) sits on the markers with no overshoot or wiggle; the
right tool set was used and recorded. No surviving divergence.

## What I checked and how

Re-rendered nothing stale — I re-traced the paper crop pixels myself with
`build_calibration` + `crop_region` and compared to the JSON.

### Calibration (highest-severity class) — CONFIRMED
- Independently located y-axis ticks: dark rows left of the axis at **17 / 150 / 283
  = 1.0 / 0.1 / 0.01** — exactly the digitizer's y anchors. Log-y confirmed.
- Marker columns 267/287/308/328/348/368/388 map to **2.42 / 3.39 / 4.83 / 6.76 /
  9.46 / 13.23 / 18.52 %** with a consistent **×1.41 (√2)** step — log-x confirmed,
  and `x_range [2.4, 18.6]` matches the actual plotted marker span. (The extracted-
  data spec's nominal "3..50%" differs; the digitization correctly follows the IMAGE.
  Any grid mismatch is a model/spec question, flagged as a caveat — correct routing.)

### Curve shape / values — MATCH (independent re-trace)
Windowed topmost-cluster centroid per digitized x, vs JSON (Δ = pixel − JSON):

| curve | apex (18.5%) JSON | apex pixel | max |Δ| over 7 pts |
|-------|------|------|------|
| 0% (filled sq)  | 0.6952 | 0.6952 | 0.0022 |
| 5% (open sq)    | 0.6375 | 0.6375 | 0.0147 (open-marker midpoint) |
| 10% (filled tri)| 0.5746 | 0.5746 | 0.0016 |
| 20% (open circ) | 0.3418 | 0.3360 | 0.0058 |

All within marker radius; the larger 5%/20% residuals are the disclosed open-marker
edge-midpoint ambiguity (~±1 px ≈ ±2% in log-y), not drift.

### Plateau / normalization — CORRECT, and a real trap avoided
The 0% (unmasked) CRF **plateaus at ~0.70, NOT 1.0**, in the paper — I confirmed this
on the pixels, with the y=1.0 tick independently anchored. The digitizer recorded it
**as drawn** and did NOT auto-normalize any curve to 1.0. This is the correct call:
pinning to 1.0 would have been DIGITIZATION-DIVERGENT.
Note: digitized values sit systematically below Eq.13 with R_max=1 (0% apex 0.70 vs
0.97). This gap is a model/normalization (R_max scaling) question, **out of
digitization scope** — the ruler faithfully reflects the paper's plotted heights.

### Completeness / ordering — CORRECT
Four curves present, right symbol→condition mapping (filled square=0%, open square=5%,
filled triangle=10%, open circle=20%). Feet crop confirms the divisive-suppression
ordering top→bottom 0% > 5% > 10% > 20% and the leftward/downward lateral shift.

### Shipping overlay (necessary-but-not-sufficient check) — CLEAN
Zoomed the overlay at the apex/convergence and the feet. The PCHIP traces sit on the
markers, saturate smoothly with **no overshoot, no spike, no non-monotone wiggle**
where 0%/5% bunch; red(0%) ends at ~0.70 (not pinned to 1.0). The picture a reader
sees is faithful.

## Process (Step 2) — TOOL FIT OK, RECORDED
- `provenance` block present and detailed (figure_type, tools, rationale, calibration
  anchors, per-curve method, normalization, caveats).
- Correctly identified this as a **discrete-marker** plot and that
  `trace_darkest_in_band` would merge the four bunched curves near saturation;
  substituted per-marker centroid detection (windowed darkness / scipy.ndimage).
  Legitimate tool-fit decision, recorded — not a silent fallback.
- **No eyeball tell**: x-samples are the actual marker columns (not a round grid),
  values are not suspiciously round, calibration step present, overlay validated. My
  pixel re-trace reproduced the values, confirming extraction (not estimation).

## Refute pass
- Plateau<1.0 → confirmed real paper feature (y=1.0 tick anchored independently). Not a finding.
- col-388 apex confusion (0.73/0.59/0.36) → my own single-column read crossing curves;
  windowed per-marker trace resolved it to the JSON. Not a finding.
- Open-marker residuals ~0.015 → within disclosed midpoint uncertainty. Not a defect.

No findings survive. Digitized reference is faithful to the paper and was produced
with the right, recorded tools. (Routing/trust decision is the organizer's; I do not
green the figure.)
