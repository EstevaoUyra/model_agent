# Digitization Audit — heeger_1992 Figure 5 (panels C, D)

- **Date:** 2026-06-03
- **Auditor role:** figure-digitization critic (`skills/audit-digitization`); NOT the digitizer.
- **Model/submodule HEAD:** heeger_1992 @ a95210d ("Digitize Figure 5 panels C and D")
- **Inputs read:** `article_aware/figures/figure_5.png`, `figure_5.md`, `figure_5_visual_checklist.md`,
  `figure_5/panel_c_crop.png`, `panel_d_crop.png`, `panel_c_digitized.json`, `panel_d_digitized.json`,
  committed overlays `panel_{c,d}_overlay.png`.
- **Method:** Re-rendered the reference fresh from the JSON + its declared calibration onto the paper
  crops (Step 0); compared adversarially against the paper ink; zoomed apex/feet/edges with
  `crop_region` on the *paper crops* (calibration matches those, not the matplotlib overlay frame);
  and made independent pixel-row measurements of the panel-D dotted line, apex, and edge markers to
  quantify the gap. Verified the shipping overlay is byte-identical to the fresh re-render.

---

## Panel 5C — model orientation tuning — **FAITHFUL-DIGITIZATION**

What I checked against the paper, and how it was made:

- **Apex height (binding).** Pixel-measured the center peak-marker in `panel_c_crop.png`: marker
  center → y ≈ 0.715, top of ink → ≈ 0.723. Digitized peak is 0.718–0.720 at x=88–90. Within ~0.005.
  The provenance's +0.01 hand-nudge from the raw line trace to the marker centers is justified and small.
  The apex `crop_region` zoom shows the slightly-flattened top (center + two ~0.70 flankers at 80/100 deg)
  rendered correctly, not as a pointy spike.
- **Both feet land at baseline.** Left-foot zoom (x 25–60): curve reaches ~0 near x=33–35 (digitized
  35→0.005). Right-foot zoom (x 118–155): reaches baseline near x=140–148 (digitized 140→0.007 … 150→0.0).
  Symmetric, lands on the x-axis.
- **Shape across the whole range.** Fresh overlay sits on the ink along both steep flanks and the
  rounded apex; smooth symmetric bell about ~88–90 deg; not flat, not monotone. PCHIP keeps the apex
  rounded.
- **Frame & normalization.** Linear/linear, peak recorded AS DRAWN at ~0.72 (NOT pinned to 1.0); shares
  the arbitrary-units scale with panel D (D's dotted base-alone ~0.65 sits just below C's ~0.72 peak),
  consistent with one model's responses. No per-panel normalize-to-max artifact.
- **Process / tools.** Full provenance block present. Composable Mode-1 set
  (detect_plot_box → build_calibration off tick pixels → trace_darkest_in_band, with apex/flanks
  re-traced on upscaled `crop_region` windows where the whole-panel band trace dipped spuriously across
  the rounded plateau → resample_pchip → overlay). No eyeball tell: y-values are irregular
  (0.251, 0.513, 0.666), sampled at the drawn markers, not a dozen round evenly-spaced guesses.

Non-defect notes (documented, routed to organizer, not divergences):
- `x_range [10,170]` is the *axis tick* range; the curve occupies ~35–148 deg (zero outside). Caveat says
  so explicitly — the tier test should clip to the curve's domain, not score the flat zero tails.
- The drawn bell reads ~56 deg FWHM, wider than the 60-deg-operator V_e^2 response (~42 deg) the spec calls
  faithful. This is the paper-internal **SUSPECTED-PAPER-ISSUE (SQ-005)**, a model/spec question. The
  reference is correctly digitized FROM THE IMAGE; this is not a digitization defect.

## Panel 5D — model cross-orientation suppression (sigma=0.03) — **FAITHFUL-DIGITIZATION**

Independent pixel measurements in `panel_d_crop.png` (calibration: y 0.0→row 366, 0.6→row 128):

- **Dotted base-alone line → y ≈ 0.650.** Horizontal dark band peaks at row 108 (darkfrac 0.46),
  calibration maps row 108 → 0.650. Digitized base_alone = 0.650. Exact.
- **Solid-curve apex → y ≈ 0.676** (marker center, rows 95–101). Digitized peak 0.678 at x=90. Within ~0.002.
- **Left/right edge shoulders → y ≈ 0.337** (marker x≈33). Digitized 0.340. Within ~0.003.
- **Suppression signature (the panel's whole point) is preserved.** The apex `crop_region` zoom shows
  exactly 3 top markers (80/90/97 deg) poking ABOVE the dotted line and the curve BELOW it everywhere
  else — the small-sigma regime, NOT the entirely-above-line larger-sigma regime of Fig 6B. Broad
  single peak, dips symmetrically toward the edges; not flat, not a narrow spike.
- **Frame & normalization.** Linear/linear; AS DRAWN (edges 0.34, dotted 0.65, peak 0.678), not pinned
  to 1.0, same scale as panel C.
- **Process / tools.** Full provenance. Correct handling of the hard parts: the dotted line lies across
  the curve plateau and an in-panel legend sits mid-panel, so the whole-panel band trace was (correctly)
  NOT trusted — the dotted line was isolated by its own row, and the 15 solid-curve markers were read
  individually off a data-grid overlay at the markers' own ~7–10 deg orientations. `base_alone` is
  correctly stored as two constant endpoints (a reference level, not a swept curve). Composable tool set,
  no eyeball tell.

Non-defect note: `x_range [10,170]` is the tick range; curve occupies ~33–147 deg — same domain-clipping
note as panel C.

---

## Cross-cutting checks

- **Step 0 freshness:** the committed `panel_{c,d}_overlay.png` are **byte-identical** (mean abs pixel
  diff = 0.0) to my fresh re-render from the JSON + declared calibration. The shipping overlay faithfully
  represents the digitized data; no stale render, no hidden second calibration / PCHIP drift between data
  and the picture a reviewer sees.
- **Calibration sanity:** apex (C ~0.715, D ~0.676), feet/edges (D edge ~0.337), and the D dotted line
  (~0.650) all land at the digitized data values within ≤0.005, corroborating the tick calibration on
  both axes from multiple independent loci — no log-as-linear, offset-origin, or swapped-range error.
- **Completeness/mapping:** C = one bell curve (correct). D = one solid base+mask curve + one dotted
  base-alone reference (correct count and style→condition mapping). Panels A/B (Bonds 1989 empirical) are
  correctly excluded from the model-panel scope.

## Refute pass (findings I considered and dismissed)

- *"Apex nudged up — is that fabrication?"* No: measured marker center ≈ 0.715 vs digitized 0.718–0.720;
  the nudge moves the line from its trace to the marker centers, ≤0.005, documented. Faithful.
- *"x_range [10,170] vs curve span 35–148 — a calibration error?"* No: it is the axis tick range,
  explicitly caveated; the data points themselves are correctly placed. A domain-clipping routing note
  for the tier test, not a divergence.
- *"D curve above the line — wrong regime?"* No: only the 3 preferred-orientation markers exceed the
  dotted line; the curve is below it everywhere else — the correct small-sigma suppression signature.

## Verdict

Both model panels (**5C**, **5D**) are **FAITHFUL-DIGITIZATION** on every binding dimension (axis
calibration, apex/plateau height, flank/foot shape, completeness, frame, normalization) AND were produced
with the right composable Mode-1 tool set with a complete, honest provenance trail. No
DIGITIZATION-DIVERGENT, no TOOL-MISUSE, no BLOCKED finding. The only mismatch with the drawn panel (C's
~56 vs ~42 deg width) is the pre-existing **SUSPECTED-PAPER-ISSUE SQ-005** — a model/spec matter the
digitization correctly defers by tracing the image, not the operator math.

Per skill: I do not declare the reference "trusted" or green the figure. The two domain-clipping notes
(`x_range` is tick-range, not curve span; `base_alone` is a constant) are handed to the organizer to route
into how the tier tests consume this reference. Routing/gating is the organizer's.
